[APPLY-780] feat(layout): 브릿지 페이지 전용 헤더 로고 적용

- 브릿지 페이지 진입 시 전용 로고 이미지가 노출되도록 `AppLogo` 컴포넌트를 개선함.
- `app-logo.tsx`:
  - `usePathname`을 사용하여 현재 경로가 브릿지 페이지인지 감지하는 로직 추가.
  - `LOGO_CONFIG` 상수를 정의하여 페이지 타입별(DEFAULT, BRIDGE) 이미지 소스, 너비, 여백을 관리하도록 구조화.
  - 브릿지 페이지일 경우 전용 로고(`header_logo.svg`)를, 그 외에는 2026학년도 공통 로고를 렌더링하도록 분기 처리.

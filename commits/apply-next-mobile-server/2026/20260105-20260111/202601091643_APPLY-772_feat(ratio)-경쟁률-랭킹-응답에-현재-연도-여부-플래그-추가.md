[APPLY-772] feat(ratio): 경쟁률 랭킹 응답에 현재 연도 여부 플래그 추가

- 클라이언트에서 서버 기준의 정확한 데이터 시점(올해/작년)을 파악할 수 있도록 API 응답에 `isCurrentYear` 필드를 추가함.
- **주요 변경 사항:**
  - **DTO 확장:**
    - `BaseRatioRankingListResponseDto`에 `isCurrentYear` boolean 필드를 추가하고 Swagger 문서를 적용함.
  - **서비스 로직 개선:**
    - `RatioRankingService`의 모든 랭킹 조회 메서드(`getTopUniversityRankings`, `getTopMajorRankings`, `getClickBasedRankings`, `getRankingsWithFallback`)에서 데이터 소스의 연도와 기본 서비스 연도를 비교하여 `isCurrentYear` 값을 정확하게 계산하도록 수정함.
    - 폴백 체인 실행 시에도 각 데이터 소스의 연도 정보를 바탕으로 플래그를 설정함.
  - **문서 및 테스트:**
    - `HomeController`의 Swagger 설명에 `isCurrentYear` 필드 정보를 추가함.
    - 서비스 테스트 코드에 `isCurrentYear` 값 검증 로직을 추가하여 신뢰성을 확보함.

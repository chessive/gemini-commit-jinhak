feat(crawler): 데이터 컬럼 기준 역방향 그룹화 로직 구현 (Case 2)

- 모집단위(`MajorName`)가 아닌 데이터 컬럼(`Mojip`, `Jiwon`, `Ratio`)에 `rowspan`이 적용된 케이스(Case 2)에 대한 그룹화 로직 추가
  - 여러 모집단위가 동일한 경쟁률 데이터를 공유하는 경우(예: 통합 선발), 해당 모집단위들을 쉼표로 연결하여 하나의 행으로 병합
- `getDataColumnMaxRowspan` 메서드 추가
  - Mojip, Jiwon, Ratio 컬럼 중 가장 큰 rowspan 값을 찾아 그룹화 기준으로 사용
- `groupRowspanRows` 내 `processedRowIndices` Set 도입으로 행 중복 처리 방지 강화

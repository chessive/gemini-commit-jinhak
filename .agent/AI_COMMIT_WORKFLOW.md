# AI 협업 커밋 워크플로우 (AI Collaboration Commit Workflow)

이 문서는 사용자와 AI 에이전트가 협업하여 복잡한 코드 변경 사항을 논리적인 단위로 분리하고, 체계적으로 커밋하기 위한 표준 절차를 정의합니다.

## 🎯 목적

- 방대한 변경 사항을 논리적 단위(Atomic Commit)로 분리하여 코드 리뷰와 이력 관리를 용이하게 함.
- AI가 코드 분석, 파일 스테이징(`git add`), 커밋 메시지 작성을 전담하고, 사용자는 최종 검토 및 승인(커밋 실행)을 담당함.
- 프로젝트 간 일관된 커밋 프로세스 유지.

---

## 🔄 워크플로우 개요

1.  **[User] 사전 준비**: 모든 변경 사항을 스테이징(`git add .`)하고 AI에게 요청.
2.  **[AI] 분석 및 계획**: 변경 내역 분석 후 커밋 분리 전략 수립.
3.  **[AI] 실행 (Loop)**:
    - 스테이징 재설정 (`git restore` -> `git add`)
    - 커밋 메시지 파일 생성 (`commits/...`)
4.  **[User] 검토 및 커밋**: 생성된 메시지 파일로 커밋 실행 (`git commit -F ...`).

---

## 📝 상세 프로세스

### 1. 사용자 요청 (Initiation)

사용자는 작업한 코드를 모두 스테이징한 상태에서 AI에게 다음 정보를 제공하며 요청합니다.

*   **필수 정보**:
    *   대상 프로젝트 경로 (예: `/Users/jhapply.devs/Projects/apply-next-mobile-server`)
    *   Jira 이슈 키 (예: `APPLY-790`)
*   **선택 정보**:
    *   참고할 문서 경로 (예: `.agent/completed/...`)

**프롬프트 예시:**
> "프로젝트 A의 APPLY-123 이슈 작업을 완료했어. 현재 git stage에 올라간 코드를 분석해서 논리적으로 커밋을 나눠주고, 각 단계별로 스테이징과 커밋 메시지 파일을 생성해줘."

### 2. AI 분석 및 전략 수립 (Analysis & Strategy)

AI는 다음 도구를 사용하여 컨텍스트를 파악합니다.
1.  `git status`, `git diff --staged`: 변경된 파일 목록 및 내용 확인.
2.  `jira_get_issue`: 요구사항 및 작업 범위 파악.
3.  `read_file` (옵션): 아키텍처 문서나 구현 리포트 참고.

**분리 전략 수립 기준 (권장 순서):**
1.  **Base/Chore**: 설정, 상수, Enum, 인터페이스, 유틸리티 등 의존성이 없는 기반 코드.
2.  **Core/Refactor**: 핵심 로직 변경, 레거시 청산, 구조 개선.
3.  **Feature**: 실제 비즈니스 로직, 컨트롤러, API 엔드포인트 구현.
4.  **Test/Docs**: 테스트 코드, 문서화, 주석 정리.

### 3. 실행 루프 (Execution Loop)

AI는 수립된 전략에 따라 커밋 단위별로 다음 과정을 반복합니다.

#### Step 3-1: 스테이징 조작
AI는 `run_shell_command`를 사용하여 물리적으로 git 스테이징 영역을 조작합니다.

```bash
# 1. 기존 스테이징 해제 (초기화)
git restore --staged .

# 2. 해당 커밋에 포함될 파일만 선별적으로 스테이징
git add src/constants/index.ts src/utils/common.util.ts ...
```

#### Step 3-2: 커밋 메시지 파일 생성
AI는 `write_file`을 사용하여 컨벤션에 맞는 커밋 메시지 파일을 생성합니다.

*   **저장 경로**: `commits/[프로젝트명]/[YYYY]/[주간]/`
*   **파일명**: `[YYYYMMDDHHmm]_[이슈키]_[타입]([스코프])-[요약].md`
*   **내용**:
    ```markdown
    [이슈키] 타입(스코프): 제목

    - 상세 내용 1
    - 상세 내용 2
    ```

#### Step 3-3: 사용자에게 보고
AI는 파일 생성을 완료한 후 사용자에게 다음을 알립니다.
*   현재 단계 (예: "Commit 1: Base 설정 완료")
*   스테이징된 파일 목록 (요약)
*   생성된 커밋 메시지 파일 경로

### 4. 사용자 실행 (User Action)

사용자는 AI가 준비해둔 상태를 확인하고 실제 git 커밋을 수행합니다.

```bash
# AI가 알려준 파일 경로를 사용하여 커밋
git commit -F commits/apply-next-mobile-server/2026/.../message.md
```

사용자가 커밋을 완료한 후 "다음"이라고 응답하면, AI는 **Step 3**로 돌아가 다음 커밋 단위를 진행합니다.

---

## ✅ 체크리스트 (AI 에이전트용)

- [ ] **프로젝트 경로 확인**: 명령어를 실행할 올바른 프로젝트 루트에 있는지 확인했는가?
- [ ] **Jira 이슈 확인**: 이슈의 제목과 상세 내용을 파악했는가?
- [ ] **의존성 고려**: 컴파일 에러가 발생하지 않도록 의존성 순서대로 커밋을 나누었는가? (Base -> Core -> Feature)
- [ ] **현재 시간 동기화**: 파일명 생성 시점의 시간을 정확히 확인했는가? (`date` 명령 활용)
- [ ] **스테이징 검증**: `git add` 후 의도한 파일만 스테이징되었는지 확인했는가?

---

## 📂 디렉토리 구조 예시

```text
git-commit-generator/
├── commits/
│   └── apply-next-mobile-server/
│       └── 2026/
│           └── 20260119-20260125/
│               ├── 202601191000_APPLY-790_chore(constants)-상수-정의.md
│               ├── 202601191005_APPLY-790_refactor(service)-로직-개선.md
│               └── 202601191010_APPLY-790_test(univ)-테스트-추가.md
└── .agent/
    └── AI_COMMIT_WORKFLOW.md  <-- (본 문서)
```

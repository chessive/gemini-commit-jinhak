test(univ-service): 유니브 서비스 테스트 커버리지 100% 달성 및 리팩터링

- `univ-service` 모듈 전체(Service, Controller, Repository, Constants)에 대한 테스트 코드를 작성하여 테스트 커버리지 100%를 달성함.
- `automock`과 `jest`를 활용하여 다양한 시나리오(시간 기반 필터링, 정렬, 외부 API 연동 실패 등)와 엣지 케이스를 검증함.
- **주요 변경사항**:
  - `UnivServiceRepository`: 비동기 처리(`async/await`)를 명시적으로 적용하고 단위 테스트를 추가함.
  - `UnivServiceService`: 정렬 전략 선택 로직을 간소화하고, `fetchCategoryTypeResponse`의 Null 처리를 개선함 (`||` → `??`).
  - `package.json`: `test:univ-service` 스크립트에 `--runInBand`를 추가하여 테스트 실행 안정성을 높임.

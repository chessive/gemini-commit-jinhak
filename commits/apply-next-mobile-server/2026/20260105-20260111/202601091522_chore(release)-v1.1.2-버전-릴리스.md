chore(release): v1.1.2 버전 릴리스

- 패키지 버전을 1.1.2로 업데이트하고 릴리스 노트를 추가함.
- **주요 변경 사항 (v1.1.2):**
  - **경쟁률 모듈 (Added/Changed):**
    - 시간 기반 상태 머신으로 경쟁률 랭킹 폴백 로직 구현 (`getRankingsWithFallback` 리팩토링).
    - `AdmissionPhase`, `RankingFallbackChain`, `ServiceInactive` 모드 등 폴백 시스템 기반 구조 추가.
    - 폴백 유틸리티를 `utils` 디렉토리로 이동하여 구조 개선.
  - **내부 관리 모듈 (Added/Changed/Fixed):**
    - `AdmissionPeriodService.getCurrentPeriodStatus()` 등 입시 기간 상태 판정 기능 추가.
    - 활성 카테고리 결정 로직 리팩토링으로 복잡도 70% 감소.
    - 입시 기간 판정 관련 버그(기본 카테고리, 연도 경계, endDate 경계 등) 수정.

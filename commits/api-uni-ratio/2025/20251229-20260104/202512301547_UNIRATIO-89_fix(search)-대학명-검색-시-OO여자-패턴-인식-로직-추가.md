[UNIRATIO-89] fix(search): 대학명 검색 시 'OO여자' 패턴 인식 로직 추가

- `UniversitySearchClassifier` 클래스 내 대학명 식별 로직에 `^.+여자$` 정규식 패턴을 추가하여, '덕성여자', '서울여자' 등 '여자'로 끝나는 대학명도 올바르게 인식하도록 개선함
- 기존에는 '대학교', '대', '대학'으로 끝나는 단어만 대학명으로 처리되어, 'OO여자' 형태의 검색어가 누락되거나 잘못 분류되는 문제를 해결함